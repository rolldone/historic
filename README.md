# Historic CLI

Historic stores work history as portable Markdown files and exposes a local CLI.

## Development

```text
go test ./...
go build -o historic .
```

The command surface is being implemented incrementally according to the project work orders. The `version` command and package skeleton are available in WO 01.
