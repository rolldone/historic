package config

const (
	// VersionName is the human-readable semver string.
	VersionName = "0.3.0"
	// VersionCode is the monotonically increasing integer release number.
	VersionCode = 3

	// WorkspaceFormatVersion identifies the on-disk .historic workspace layout.
	WorkspaceFormatVersion = 1
	// IndexSchemaVersion is the schema required by this binary.
	IndexSchemaVersion = 3
)
